.vercel
.env
.env.local
.env*.local
node_modules
.DS_Store
*.log

# Agent-team review tooling (session-mint helper etc. — never commit)
.claude/

# DB split (operational scripts + local backups — never deploy)
_dbsplit/
bradley_site_backup_*.json

# Cloud credentials / key files (bradley-security §1 — never commit key material)
*service-account*.json
*-sa.json
gsc-*.json
*credentials*.json
/secrets/

# Next.js
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env*
